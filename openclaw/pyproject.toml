[project]
name = "openclaw-telegram-bot"
version = "0.1.0"
description = "Telegram bot bridge that forwards messages to OpenClaw."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
openclaw-telegram-bot = "openclaw_telegram_bot.main:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
