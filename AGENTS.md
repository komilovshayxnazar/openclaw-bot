1.Telegram bot was made
2.Bot token is kept in .env file as a *TELEGRAM_BOT_TOKEN*
3.Integrate the bot with openclaw
4.Openclaw's response should be appeared in the bot too
5.User cannot send 5 messages to bot during 1 hour, if the user hit limit, bot must response to user "You can message after **-**-**" (stars means time)
6.Make a function that using OpenClaw dashboard, admin can send message to every user.